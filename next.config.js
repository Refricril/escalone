/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {}, // Remova quaisquer configurações não reconhecidas
  reactStrictMode: true,
  swcMinify: true,
};

module.exports = nextConfig;
